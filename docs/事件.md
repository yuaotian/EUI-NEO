# 事件系统

事件类型位于 `core/input/input_types.h`，窗口相关输入队列和后端回调位于 `core/input/input_state.h`。内部 Runtime 在每次 update 时统一驱动输入状态，页面和组件不直接读 GLFW/SDL2 鼠标状态，也不自己维护 hover / pressed。

## PointerEvent

```cpp
struct PointerEvent {
    double x;
    double y;
    double deltaX;
    double deltaY;
    bool down;
    bool pressedThisFrame;
    bool releasedThisFrame;
    bool rightDown;
    bool rightPressedThisFrame;
    bool rightReleasedThisFrame;
};
```

- `x / y`：当前指针坐标，已按 `pointerScale` 转换到 framebuffer 命中测试坐标。
- `deltaX / deltaY`：与上一次读取位置的差值。
- `down`：鼠标左键当前是否按下。
- `pressedThisFrame`：本帧从未按下变为按下。
- `releasedThisFrame`：本帧从按下变为未按下。
- `rightDown` / `rightPressedThisFrame` / `rightReleasedThisFrame`：鼠标右键状态。

当前封装了鼠标 pointer、右键上下文菜单、文本输入、IME 预编辑组合串、滚轮和拖拽事件。IME 光标矩形和 Windows 组合窗口字号会同步到输入框；原生多指针和自绘 IME 候选 UI 还没有下沉。

## InteractionState

```cpp
struct InteractionState {
    bool hover;
    bool pressed;
    bool clicked;
    bool pressStarted;
    bool released;
    bool drag;
    bool active;
    bool changed;
    double dragStartX;
    double dragStartY;
    double dragDeltaX;
    double dragDeltaY;
};
```

- `hover`：当前指针命中该元素，并且它是最上层命中的 interactive 元素。
- `pressed`：该元素处于按下捕获状态，且鼠标左键仍按住。
- `clicked`：在该元素上按下，释放时仍回到该元素范围内。
- `pressStarted`：本帧在该元素上开始按下。
- `released`：本帧释放了该元素的按下捕获。
- `active`：该元素捕获了当前按下序列。
- `drag`：按下后移动超过 2 像素。
- `changed`：交互状态在本帧发生变化。

按下捕获是底层交互系统的一部分：在某个元素上按下后，拖动期间不会让其它元素抢走这次按下序列。拖出再释放不会触发 click，拖出后再拖回元素内释放会触发 click。

## DSL 写法

通用 builder 现在支持交互：

```cpp
ui.text("link")
    .size(240.0f, 32.0f)
    .text("Open project")
    .interactive()
    .onClick([] {
        eui::platform::openUrl("https://github.com/sudoevolve/EUI-NEO");
    })
    .build();
```

`Row / Column / Stack / Flow / Rect / Polygon / Text / Image / Svg` 都可以通过 `.interactive()` 和 `.onClick(...)` 进入命中测试。`.onClick(...)` 会自动开启 interactive，并把 cursor 设置为手型。

通用交互方法：

```cpp
.interactive(true)
.blockPointer()
.disabled(false)
.cursor(eui::CursorShape::Hand)
.hitTestMode(eui::dsl::HitTestMode::Transformed)
.transformedHitTest()
.onClick(callback)
.onPress(callback)
.onRelease(callback)
.onMove(callback)
.onContextMenu(callback)
.onHover(callback)
.focusable()
.onFocusChanged(callback)
.onTextInput(callback)
.onScroll(callback)
.onDrag(callback)
```

`onPress` 在指针按下时触发，适合需要立即响应的按压操作；`onRelease` 在一次有效按压结束、指针松开时触发。`onClick` 使用标准按钮语义，只有指针在元素内按下并在元素内松开才触发，因此业务状态默认在松开时更新，而不是在按下时更新。

底层 DSL 回调直接暴露 Runtime 的原始事件和布局 bounds。默认命中按布局矩形判断；开启 `.transformedHitTest()` 后，Runtime 会把屏幕坐标通过当前 transform 和父容器继承矩阵反投影回元素坐标，再判断是否命中。`HitTestMode::None` 只跳过元素自身命中，不会禁用子树。

Runtime 命中测试按绘制顺序从最上层往下找，命中第一个符合条件的元素后停止。因此同一位置不会同时触发多层 interactive 元素的 hover / click。focus 命中也会被上层 interactive 元素阻断：如果最上层命中的是 sidebar scrim、modal panel 背景、透明 hit rect 这类非 focusable interactive blocker，Runtime 会清空焦点并停止继续查找，后面的 input 不会被点击穿透。

`.disabled(true)` 会禁用当前元素及其整棵子树。禁用子树不会参与 hover / click / scroll / focus 命中；如果旧焦点或旧按下捕获落在禁用子树里，Runtime 会在更新时让它失效，隐藏 dialog、popover、drawer 时不需要给每个子控件单独写禁用判断。

普通按钮、菜单项和简单 hover 视觉应由自身可视图元直接绑定 DSL 回调。需要局部逻辑坐标、拖拽阈值、滚轮步进、右键或 click-after-drag 抑制时，再使用组件层的 `components::mouseArea(...)`；它会把坐标转换成局部逻辑坐标，并封装 tap、press、release、hover、move、drag、scroll、context menu。

```cpp
components::mouseArea(ui, "stage.input")
    .size(width, height)
    .scrollStep(0.18f)
    .dragThreshold(3.0f)
    .onTap([](const components::MouseEvent& event) {
        // event.x / event.y 是当前热区内的逻辑坐标
    })
    .onMove([](const components::MouseEvent& event) {
        // hover 时持续返回当前热区内逻辑坐标
    })
    .onDrag([](const components::MouseDragEvent& event) {
        // event.totalX / event.totalY 是从按下开始的逻辑位移
    })
    .onScroll([](const components::MouseScrollEvent& event) {
        // event.stepY 是按 scrollStep 和 maxScrollStep 处理后的步进
    })
    .build();
```

## 热区选型与命名规范

Runtime 统一负责命中测试、clip、disabled、transform、按下捕获和 click/release 派发；组件不要自行读取原生鼠标，也不要保存 `InteractionState`。

| 场景 | 推荐写法 |
| --- | --- |
| 按钮、菜单项等可视控件本身就是热区 | 在可视图元上直接使用 `.onClick(...)`、`.onPress(...)` 等 DSL 回调 |
| 透明或独立热区，或需要局部坐标、拖拽、滚轮、右键 | `components::mouseArea(ui, id)` |
| 弹窗、下拉菜单、侧栏面板等只需阻止事件穿透 | `.blockPointer()` |

`.blockPointer()` 只表示参与命中并阻止底层 pointer，不表示业务动作，光标保持 Arrow。需要连滚轮也拦截时，再附加 `.onScroll([](const core::ScrollEvent&) {})`。不要使用 `.onClick([] {})` 作为透明阻挡层。

热区 ID 使用语义后缀：独立输入区用 `.input`，透明阻挡层用 `.hit` 或 `.block`，遮罩用 `.backdrop` 或 `.dismiss`。可视控件自身就是热区时，沿用该控件 ID，不额外复制一层透明热区。

## Rect 状态

只有 `Rect` 目前内置 hover / pressed 视觉状态：

```cpp
ui.rect("button.bg")
    .size(240.0f, 70.0f)
    .states(normal, hover, pressed)
    .transition(0.16f, eui::Ease::OutCubic)
    .onClick(callback)
    .build();
```

`.states(normal, hover, pressed)` 会设置：

- normal color
- hover color
- pressed color
- `interactive = true`
- 手型 cursor

Runtime 使用 `SmoothedValue<float>` 维护 hover / press blend，再混合颜色：

```text
normal -> hover -> pressed
```

## 组件中的事件

组件不要自己读鼠标，也不要保存 `InteractionState`。点击和简单 hover 视觉可以直接交给 Runtime；涉及拖拽、滚轮、局部坐标或 click-after-drag 抑制时，优先封装一层 `components::mouseArea(...)`。

```cpp
ui.stack(id)
    .size(w, h)
    .visualStateFrom(id + ".bg", 0.965f)
    .content([&] {
        ui.rect(id + ".bg")
            .size(w, h)
            .states(normal, hover, pressed)
            .onClick(onClick_)
            .build();

        ui.text(id + ".label")
            .size(w, h)
            .text(text_)
            .build();
    })
    .build();
```

`components::button(ui, id)` 就是这种写法：内部背景 rect 负责状态和点击，外层 stack 用 `visualStateFrom` 跟随按压缩放。

## Runtime 流程

每次 update 大致流程：

1. `readPointerEvents(window, pointerScale)` 按消息顺序读取 pointer 边沿，并在末尾追加最终 hover 状态。
2. `consumeInputEvents()` 消费 GLFW char / key / scroll callback 收集到的输入。
3. 鼠标按下时按 topmost focus hit-test 更新 focused element；上层 interactive blocker 会阻断到底层 input 的焦点穿透。
4. 从 DSL 树中找出最上层命中的 interactive 元素；如果已有 active 捕获元素，则优先使用捕获元素。
5. 更新每个 interactive 元素的 `InteractionState`，但只有 topmost target 会进入 hover / press / click。
6. 派发 `onHover` / `onMove` / `onPress` / `onRelease` / `onClick` / `onContextMenu` / `onDrag` / `onScroll` / `onTextInput`，并在需要时设置 `composeRequested()`。
7. 根据 hover 设置 GLFW 手型 cursor。
8. Rect 根据交互状态推进 hover / press 缓动并标记 dirty rect。

## 外部链接

跨平台打开 URL 使用 `eui/platform.h`：

```cpp
#include "eui_neo.h"

eui::platform::openUrl("https://github.com/sudoevolve/EUI-NEO");
```

当前实现：

- Windows：`ShellExecuteA`，CMake 链接 `shell32`。
- Linux：调用 `xdg-open`。
- macOS：调用 `open`。

`openUrl` 返回 `bool` 表示是否成功发起打开动作。它不等待浏览器或外部程序结束。

## 刷新分层

页面代码只需要关心 `app::requestUpdate()`：当自建后台线程、socket 或其他外部状态变化需要 UI 更新时，调用它请求下一帧重新 compose。

内部按层级区分：

- `core::platform::requestFrame()`：只唤醒事件循环并安排下一帧。
- `Runtime::composeRequested()`：Runtime 内部请求重新 compose。
- `Runtime::requestFullPaint()`：Runtime 内部请求整屏重画。
- dirty rect：普通 hover / press / transition / scroll 等视觉变化只标记局部脏区。

## 与脏区渲染

交互状态变化不会默认整页重绘：

- hover / press：只标记对应 rect 的 dirty rect。
- transition：标记旧视觉区域和新视觉区域的 union。
- click 修改 app 状态：Runtime 设置 `composeRequested()`，`include/eui/dsl_app.h` 重新 compose，并保守触发 full paint。

## 当前限制

- 已有基础键盘文本输入、focus manager、选择、剪贴板、撤销、重做、IME 预编辑组合串和系统 IME 候选窗口定位；自绘 IME 候选 UI 还没做。
- 没有事件冒泡和捕获阶段。
- 原生多点触摸还没做；触摸屏如果由系统映射成鼠标事件，可以走现有 pointer / `mouseArea`。
- 默认 hit-test 按布局矩形计算；开启 `.transformedHitTest()` 后会按元素当前 transform 和父容器继承矩阵反投影命中。
- 弹层、侧边栏、菜单等覆盖层需要显式声明 interactive blocker，例如透明 scrim 或 panel 背景 hit rect，用来阻断下层 hover / click / focus。
- Text / Image 可以接收 click，但没有内置 hover / pressed 视觉状态；需要视觉状态时通常叠一个 rect 或用组件封装。
